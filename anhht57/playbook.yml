- name: Update api servers
  hosts: apiserver
  remote_user: ubuntu

  tasks:
  - name: Up
    shell: "docker run -d --name api -p 4444:5444 -p 5444:4444 anhht/repo:latest"

- name: Update lt servers
  hosts: loadtestserver
  remote_user: ubuntu

  tasks:
  - name: Up
    shell: "docker-compose up -d influxdb grafana"